# Scripts TODO

## Overlay during layout setup
- [ ] Add a fullscreen overlay that blocks user interaction (clicks, keyboard) while the workspace layout script is running — prevents the user from accidentally interfering with the automation
- [ ] Show a loading/progress indicator on the overlay (e.g. "Setting up workspace...")
- [ ] Dismiss the overlay automatically when the AppleScript finishes
